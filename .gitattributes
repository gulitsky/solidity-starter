# Auto detect text files and perform line endings normalization
* text=auto

# Auto detect Solidity source files and perform syntax highlighting
*.sol linguist-language=Solidity

# Ignore files that are not required for build when exporting repository to archive
/.vscode/      export-ignore
/.editorconfig export-ignore
/.git*         export-ignore
/.solhint*     export-ignore

# Do not try to diff and merge these files
yarn.lock -diff
